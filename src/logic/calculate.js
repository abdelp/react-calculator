import PropType from 'prop-types';

const calculate = (data, bntName) => {
  switch(bntName) {
    case 'AC':

      break;
    case '-':

      break;
    case '/':

      break;
    case:

      break;
    case:
  }
};

calculate.propType = {
  total: PropType.number.isRequired,
  next: PropType.number.isRequired,
  operation: PropType.string.isRequired
};

export default calculate;